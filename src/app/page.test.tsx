import { describe, expect, test } from 'vitest';
import '@testing-library/jest-dom';

describe('Testing page App', () => {
  test('should renders Page component', () => {
    expect(true).toBe(true);
  });
});
